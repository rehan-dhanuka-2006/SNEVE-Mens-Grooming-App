package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// SNEVE Brand Colors - Inspired by the official SNEVE Men's Grooming branding
val SneveRed = Color(0xFFE50914)
val SneveRedLight = Color(0xFFFF333A)
val SneveRedDark = Color(0xFFB00710)
val SneveRedGlow = Color(0x40E50914)

// Metallic & Chrome Accents
val SneveSilver = Color(0xFFF3F4F6)
val SneveChrome = Color(0xFFCBD5E1)
val SneveGold = Color(0xFFEAB308)
val SneveCyan = Color(0xFFE50914) // Aligned to brand primary

// Aliases for compatibility
val SneveEmerald = SneveRed
val SneveEmeraldLight = SneveRedLight
val SneveEmeraldDark = SneveRedDark
val SneveEmeraldGlow = SneveRedGlow
val SneveGoldGlow = Color(0x33EAB308)
val SneveRose = SneveRed

// Dark Carbon Luxury Palette (Primary Brand Experience)
val SneveBackground = Color(0xFF0A0B0E)
val SneveSurface = Color(0xFF12141A)
val SneveSurfaceElevated = Color(0xFF1C1E26)
val SneveSurfaceCard = Color(0xFF161820)
val SneveBorder = Color(0xFF2C303D)
val SneveBorderSubtle = Color(0xFF1F222B)

// Text Colors
val SneveTextPrimary = Color(0xFFF9FAFB)
val SneveTextSecondary = Color(0xFF9CA3AF)
val SneveTextMuted = Color(0xFF6B7280)

